# mise-en-place

Note: if this directory were named `mise`, then it would be sourced as if it were a repo-local mise config.
Calling it `mise-en-place` prevents mise from loading it when `cd`-ing into the repository.
