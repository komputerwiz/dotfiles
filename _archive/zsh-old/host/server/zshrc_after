### EXTRA COMPLETIONS ###

# initialize bash completion compatibility
if ! bashcompinit >/dev/null 2>&1; then
    autoload -U bashcompinit
    bashcompinit
fi

source $HOME/.local/lib/todo.txt/todo_completion
complete -F _todo t
